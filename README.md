Client Setup

1. Ensure the ERCS logo file exists at `client/public/logo.jpg`.
   - Copy it from the workspace root `logo.jpg`.

2. Create a `.env` file at project root (optional):

```
VITE_API_URL=http://localhost:4000
```

3. Install and run:

```
npm install
npm run dev
```





